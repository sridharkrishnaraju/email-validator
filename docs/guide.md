# Guide: Format validation vs deliverability

Format validation (what this does) is the cheap first step — it does not prove the mailbox exists. To confirm an address is real, send a confirmation link or run an SMTP check. The reliable pattern: validate format on input, then verify by email.

## Beyond this tool

This is one of several free developer tools from
[SGBP — Singapore Build Partners](https://sgbp.tech). If you'd like a full website built or
audited for performance, accessibility and SEO rather than a single utility, that's what the
studio does for Singapore SMEs.
